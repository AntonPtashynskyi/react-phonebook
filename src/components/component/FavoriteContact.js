import { useSelector } from 'react-redux';

export const FavoriteContact = () => {
  // const contacts = useSelector(state => state.contact);

  // const favoriteContact = contacts.filter(item => item.favorite === true);

  return (
    <div>
      <h2>Favorite contact </h2>
      <ul></ul>
    </div>
  );
};
